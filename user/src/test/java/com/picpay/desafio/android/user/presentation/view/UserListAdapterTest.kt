package com.picpay.desafio.android.user.presentation.view

import com.picpay.desafio.android.user.presentation.model.UserModel
import com.picpay.desafio.android.user.presentation.view.adapter.UserListAdapter
import com.picpay.desafio.android.user.presentation.view.adapter.UserViewHolder
import io.mockk.mockk
import io.mockk.verify
import org.junit.Assert.assertEquals
import org.junit.Test

class UserListAdapterTest {
    @Test
    fun `getItemCount should return correct item count`() {
//        val userList = listOf(UserModel(id = 1, name = "Alice", username = "alice", img = "img"))
//        val adapter = UserListAdapter()
//
//        adapter.updateUserList(userList)
//
//        assertEquals(2, adapter.itemCount)
    }

    @Test
    fun `onBindViewHolder should bind data to ViewHolder`() {
//        val userList = listOf(UserModel(id = 1, name = "Alice", username = "alice", img = "img"))
//        val adapter = UserListAdapter()
//        adapter.updateUserList(userList)
//
//        val viewHolder = mockk<UserViewHolder>()
//
//        adapter.onBindViewHolder(viewHolder, 0)
//
//        verify(exactly = 1) { viewHolder.bind(userList[0]) }
    }
}